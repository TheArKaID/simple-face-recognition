services:
  app:
    image: adminmeetaza/prime-face-recognizer:latest
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - FACE_DB_PATH=/data/faces.db
    volumes:
      - face_db:/data
volumes:
  face_db:
